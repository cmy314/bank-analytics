# 核心：离线训练 / 异常检测 / 短窗容量 / 可视化
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3

# Parquet 导出（可选）
pyarrow>=14.0

# 本地/服务器用 .env 配置路径（可选）
python-dotenv>=1.0.0

# 可选：TRY_PROPHET_V2021=true 时需安装（Windows 可能失败，可 pip install prophet）
# prophet>=1.1.5
